#include "main.h"

/**
 * _strlen - Find length of a String
 * @s: Char pointer to use
 * Return: Length of string
 */
int _strlen(char *s)
{
	int len;

	len = sizeof(*s) / sizeof(*s[0]);
	return (len);
}
